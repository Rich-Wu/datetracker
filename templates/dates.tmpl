<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hello World</title>
</head>
<body>
    <h1>User: {{ .username }}</h1>
    {{ range .dates }}
        <div>{{ .FirstName }} {{ .LastName }} {{ .Cost }}</div>
    {{ end }}
</body>
</html>